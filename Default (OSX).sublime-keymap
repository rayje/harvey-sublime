[
	// Run a single test, output to console
	{
		"keys": ["super+shift+r"],
		"command": "harvey_run_test",
		"args": {
			"all": false,
			"reporter": "console",
			"scratch": false
		}
	},

	// Run a single test, output to scratch with JSON
	{
		"keys": ["super+shift+g"],
		"command": "harvey_run_test",
		"args": {
			"all": false,
			"reporter": "json",
			"scratch": true
		}
	},

	// Run all tests, output to scratch with JSON
	{
		"keys": ["super+shift+o"],
		"command": "harvey_run_test",
		"args": {
			"all": true,
			"reporter": "json",
			"scratch": true
		}
	},

	// Run all tests, output to console
	{
		"keys": ["super+shift+a"],
		"command": "harvey_run_test",
		"args": {
			"all": true,
			"reporter": "console",
			"scratch": false
		}
	},

	// Select a test from quick panel, output to scratch with JSON
	{
		"keys": ["super+shift+i"],
		"command": "harvey_select_test",
		"args": {
			"reporter": "json",
			"scratch": true
		}
	},

	// Select a test from quick panel, output to console
	{
		"keys": ["super+shift+u"],
		"command": "harvey_select_test",
		"args": {
			"reporter": "console",
			"scratch": false
		}
	},

	// Run the last test
	{
		"keys": ["super+shift+l"],
		"command": "harvey_last_test"
	},

	// Create a scratch file
	{
		"keys": ["super+shift+x"],
		"command": "harvey_new_scratch"
	},

	// Create a scratch file
	{
		"keys": ["alt+shift+o"],
		"command": "harvey_show_panel"
	}
]